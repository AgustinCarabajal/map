// Feature flags del juego. Cambiá estos valores para activar/desactivar features.
export const FEATURE_FLAGS = {
  // Si está activa, los NPC aparecen al lado del player (útil para testear).
  // Si está desactivada, aparecen en un borde del mapa.
  npcNextToPlayer: true,
}
